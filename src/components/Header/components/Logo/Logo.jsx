import logo from '../../../../assets/img/logo.png';
import { LOGO_ALTERNATIVE_TEXT } from '../../../../constants';

const Logo = () => <img src={logo} width={100} alt={LOGO_ALTERNATIVE_TEXT} />;
export default Logo;
